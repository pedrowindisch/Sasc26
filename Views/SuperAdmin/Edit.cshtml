@model Sasc26.Models.Event
@{
    ViewData["Title"] = "Editar Evento";
}

<div class="container mt-5">
    <h1 class="title">Editar Evento: @Model.Name</h1>

    <form asp-action="Edit" method="post">
        <input type="hidden" asp-for="Id" />

        <div class="field">
            <label class="label">Slug (identificador único na URL)</label>
            <div class="control">
                <input asp-for="Slug" class="input" required />
            </div>
            <p class="help">Será usado na URL: /{slug}/admin</p>
        </div>

        <div class="field">
            <label class="label">Nome</label>
            <div class="control">
                <input asp-for="Name" class="input" required />
            </div>
        </div>

        <div class="field">
            <label class="label">Subtítulo</label>
            <div class="control">
                <input asp-for="Subtitle" class="input" />
            </div>
        </div>

        <div class="field">
            <label class="label">Domínio de E-mail Permitido</label>
            <div class="control">
                <input asp-for="AllowedEmailDomain" class="input" />
            </div>
        </div>

        <div class="field">
            <label class="label">URL do Instagram</label>
            <div class="control">
                <input asp-for="InstagramUrl" class="input" />
            </div>
        </div>

        <div class="field">
            <label class="label">URL de Pré-venda de Camisetas</label>
            <div class="control">
                <input asp-for="TshirtPresaleUrl" class="input" />
            </div>
        </div>

        <div class="field">
            <label class="label">E-mails de Admin (JSON array)</label>
            <div class="control">
                <input asp-for="AdminEmailsJson" class="input" />
            </div>
        </div>

        <div class="field">
            <label class="label">Botões Pós-Check-in (JSON array)</label>
            <div class="control">
                <input asp-for="PostCheckinButtonsJson" class="input" />
            </div>
        </div>

        <h3 class="title is-5 mt-4">Cores do Tema</h3>

        <div class="columns">
            <div class="column">
                <div class="field">
                    <label class="label">Cor Primária</label>
                    <div class="control">
                        <input asp-for="PrimaryColor" class="input" type="color" />
                    </div>
                </div>
            </div>
            <div class="column">
                <div class="field">
                    <label class="label">Cor de Destaque</label>
                    <div class="control">
                        <input asp-for="AccentColor" class="input" type="color" />
                    </div>
                </div>
            </div>
        </div>

        <div class="columns">
            <div class="column">
                <div class="field">
                    <label class="label">Cor de Fundo</label>
                    <div class="control">
                        <input asp-for="BackgroundColor" class="input" type="color" />
                    </div>
                </div>
            </div>
            <div class="column">
                <div class="field">
                    <label class="label">Cor do Texto</label>
                    <div class="control">
                        <input asp-for="TextColor" class="input" type="color" />
                    </div>
                </div>
            </div>
        </div>

        <div class="field is-grouped">
            <div class="control">
                <button type="submit" class="button is-primary">Salvar</button>
            </div>
            <div class="control">
                <a asp-action="List" class="button is-light">Cancelar</a>
            </div>
        </div>
    </form>
</div>
